<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sep.user.repository.AddressMapper">

    <select id="maxIds" resultType="java.lang.Integer">
        SELECT max(id) AS id FROM tb_address ${ew.customSqlSegment}
    </select>

</mapper>